{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm ci || npm install"
  },
  "deploy": {
    "startCommand": "cd backend && node index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
